import Layout from '../../components/Layout/Layout';

const PomodoroPage = () => {
    return (
        <Layout>
            <h1>Pomodoro</h1>
        </Layout>
    );
};

export default PomodoroPage;
